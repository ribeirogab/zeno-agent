{
  "name": "@zeno/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && tsc-alias --resolve-full-paths",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.110",
    "@slack/bolt": "^4.7.0",
    "@zeno/logger": "workspace:*",
    "@zeno/mcp-discover": "workspace:*",
    "@zeno/storage": "workspace:*",
    "cron-parser": "^5.5.0",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsc-alias": "^1.8.16",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
