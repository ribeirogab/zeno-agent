{
  "_doc": "User-level MCP servers. Reference env vars with ${VAR_NAME} — secrets live in .env. Use _disabled: true to keep an entry but skip it. Servers with unresolved env vars are silently skipped at boot (with a warning log).",
  "mcpServers": {
    "linear": {
      "_comment": "Linear (issues, projects, cycles). Get API key at https://linear.app/settings/api",
      "command": "npx",
      "args": ["-y", "mcp-linear"],
      "env": {
        "LINEAR_API_KEY": "${LINEAR_API_KEY}"
      }
    },
    "notion": {
      "_comment": "Notion (databases, pages, blocks). Create an integration at https://www.notion.so/profile/integrations and share pages with it",
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      }
    },
    "granola": {
      "_comment": "Granola meeting notes. Docs: https://www.granola.ai/blog/granola-mcp",
      "command": "npx",
      "args": ["-y", "granola-mcp"],
      "env": {
        "GRANOLA_API_KEY": "${GRANOLA_API_KEY}"
      }
    },
    "sentry": {
      "_comment": "EXAMPLE — Sentry error tracking. Set _disabled: false and add SENTRY_AUTH_TOKEN to .env to enable. Docs: https://github.com/getsentry/sentry-mcp",
      "_disabled": true,
      "command": "npx",
      "args": ["-y", "@sentry/mcp-server"],
      "env": {
        "SENTRY_AUTH_TOKEN": "${SENTRY_AUTH_TOKEN}"
      }
    }
  }
}
